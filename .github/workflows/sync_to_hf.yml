name: Sync to Hugging Face Space

on:
  push:
    branches: [master, main]
  workflow_dispatch:

jobs:
  sync-to-hub:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          lfs: true

      - name: Push to Hugging Face Space
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          # Use HF_SPACE_REPO secret if configured, otherwise default to skyris-in/skyris-models
          HF_TARGET="${{ secrets.HF_SPACE_REPO }}"
          if [ -z "$HF_TARGET" ]; then
            HF_TARGET="skyris-in/skyris-models"
          fi
          echo "Syncing to https://huggingface.co/spaces/$HF_TARGET"
          git push --force https://PrakharSinghOnGit:$HF_TOKEN@huggingface.co/spaces/$HF_TARGET HEAD:refs/heads/main
